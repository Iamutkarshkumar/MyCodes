// Assignments | Strings - 1 | Week 7
#include <bits/stdc++.h>
using namespace std;
int main(){
    //Question 1:Input a string of size n and update all the odd positions in the string to character ‘#’. Consider
    // 0-based indexing.
    // string s;
    // getline(cin,s);
    // for(int i=1;i<=s.size();i+=2){
    //     s[i]='#';
    // }
    // cout<<s;

    //Question 2:Input a string of length n and count all the consonants in the given string.
    // string s;
    // getline(cin,s);
    // int count=0;
    // for(int i=0 ; i<s.size();i++){
    //     if(s[i]!='a' && s[i]!='e'&&s[i]!='i'&&s[i]!='o'&&s[i]!='u'){
    //         count++;
    //     }
    // }
    // cout<<count;

    //Question 3: Check whether the given string is palindrome or not.
    // string s;
    // getline(cin,s);
    // int start=0;
    // int end=s.size()-1;
    // while(start<end){
    //     if(s[start]!=s[end]){
    //         cout<<"not a palindrome";
    //         break;
    //     }
    //     start++;
    //     end--;
    // cout<<"yes a palindrome";
    // break;
    // }

    //Question 4:Input a string of even length and reverse the second half of the string.
    // string s;
    // getline(cin,s);
    // int n;
    // n=s.size()/2;
    // reverse(s.begin()+n,s.begin()+s.size());
    // cout<<s;

    //Question 5:Input a string of length less than 10 and convert it into integer without using builtin function.
    
} 